<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="IntegradorApi.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IntegradorApi"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <TabView>
        <TabView.TabItems>

            <TabViewItem Header="Sincronização">
                <TabViewItem.IconSource>
                    <SymbolIconSource Symbol="Sync"/>
                </TabViewItem.IconSource>
                <TabViewItem.Content>
                    <Grid Padding="15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Row="0" Grid.Column="0"
                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10"
                    Margin="0,0,20,10"
                    MinHeight="60">
                            <ItemsControl x:Name="StatusConectionsItemsControl" HorizontalAlignment="Center">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" Spacing="15"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </Border>

                        <Grid Grid.Row="1" Grid.Column="0" Margin="0,0,20,0">
                            <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="4">
                                <ScrollViewer>
                                    <TextBlock x:Name="LogTextBlock"
                                   Padding="10"
                                   FontFamily="Consolas"
                                   TextWrapping="Wrap"
                                   Text="Iniciando sistema de log..."/>
                                </ScrollViewer>
                            </Border>
                        </Grid>

                        <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Spacing="10">
                            <Button x:Name="TestarConexoesButton" HorizontalAlignment="Stretch">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <SymbolIcon Symbol="Find"/>
                                    <TextBlock Text="Testar Conexões"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="BtnToggleSync" HorizontalAlignment="Stretch" Click="ButtonClick_ToggleSyncronize">
                            </Button>

                            <Button x:Name="BtnClearLog" HorizontalAlignment="Stretch">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <SymbolIcon Symbol="Delete"/>
                                    <TextBlock Text="Limpar Log"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>

                    </Grid>
                </TabViewItem.Content>
            </TabViewItem>

            <TabViewItem Header="Conexões">
                <TabViewItem.IconSource>
                    <SymbolIconSource Symbol="World"/>
                </TabViewItem.IconSource>
                <TabViewItem.Content>
                    <Grid Padding="20" RowSpacing="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Spacing="10">
                            <TextBlock Text="Dados da Conexão de Destino" Style="{ThemeResource TitleTextBlockStyle}"/>
                            <TextBox x:Name="TxtLocalDataBaseDescription" Header="Descrição"/>
                            <TextBox x:Name="TxtLocalDataBaseAddress" Header="Endereço do Servidor"/>
                            <TextBox x:Name="TxtLocalDataBasePort" Header="Porta"/>
                            <TextBox x:Name="TxtLocalDataBaseUser" Header="Usuário"/>
                            <PasswordBox x:Name="PswLocalDataBasePassword" Header="Senha" PasswordRevealMode="Hidden"/>
                        </StackPanel>

                        <Grid Grid.Row="1" RowSpacing="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Fontes de Dados de Origem" Style="{ThemeResource TitleTextBlockStyle}"/>

                            <controls:DataGrid Grid.Row="1"
                    x:Name="OrigemDataGrid"
                    AutoGenerateColumns="False"
                    IsReadOnly="True"
                    GridLinesVisibility="Horizontal"
                    CanUserSortColumns="True">
                                <controls:DataGrid.Columns>
                                    <controls:DataGridTextColumn Header="Tipo" Binding="{Binding TipoConexao}" Width="SizeToCells"/>
                                    <controls:DataGridTextColumn Header="Endereço" Binding="{Binding Endereco}" Width="*"/>
                                    <controls:DataGridTextColumn Header="Porta" Binding="{Binding Porta}" Width="SizeToCells"/>
                                    <controls:DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="*"/>
                                </controls:DataGrid.Columns>
                            </controls:DataGrid>

                            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10">
                                <ComboBox x:Name="OrigemTipoComboBox" Header="Tipo" PlaceholderText="Selecione">
                                    <ComboBoxItem>REST API</ComboBoxItem>
                                    <ComboBoxItem>MySQL</ComboBoxItem>
                                    <ComboBoxItem>PostgreSQL</ComboBoxItem>
                                </ComboBox>
                                <TextBox x:Name="OrigemEnderecoTextBox" Header="Endereço"/>
                                <TextBox x:Name="OrigemPortaTextBox" Header="Porta"/>
                                <TextBox x:Name="OrigemDescricaoTextBox" Header="Descrição"/>

                                <Button x:Name="AdicionarOrigemButton" VerticalAlignment="Bottom">
                                    <SymbolIcon Symbol="Add"/>
                                </Button>
                                <Button x:Name="ExcluirOrigemButton" VerticalAlignment="Bottom">
                                    <SymbolIcon Symbol="Delete"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </TabViewItem.Content>
            </TabViewItem>

        </TabView.TabItems>
    </TabView>

</Window>
